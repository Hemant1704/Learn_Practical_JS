let os = require("os");

// os is required to provide features of operating syst6em.
// As we studied earlier that js is just for interactions main functionality is provided by the modules in node js.

console.log(os.arch());
console.log(os.platform());
console.log(os.networkInterfaces());
console.log(os.cpus());
/*
This os module allows us to create applications where we can set type os os 
we need to support in our project.
*/